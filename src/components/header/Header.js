import React from 'react'

import './Header.css';

const Header = () => {
  return (
    <header>
      <span className="Header__title">nim's weather app</span>
      <span className="Header__sub-title">Where would you like to see the weather today?</span>
    </header>
  )
}

export default Header
